So, executing commandToSave.bat will save the doskeys defined inside macros.doskey file to cmd's aliases for permanently.

That's all.

You need to save these files in the bat folder in d: (d drive). Then macros saved in macros.doskey will be saved permanenetly to cmd in the windows.